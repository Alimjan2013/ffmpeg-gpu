name: Build and Push to Harbor

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Harbor
        run: echo "${{ secrets.HARBOR_PASSWORD }}" | docker login harbor1.suanleme.cn -u ff4d6pgf --password-stdin

      - name: Build Docker image
        run: |
          docker build -t ffmpeg:checkgpu .

      - name: Push to Harbor
        run: |
          docker tag ffmpeg:checkgpu harbor1.suanleme.cn/ff4d6pgf/ffmpeg:checkgpu
          docker push harbor1.suanleme.cn/ff4d6pgf/ffmpeg:checkgpu
